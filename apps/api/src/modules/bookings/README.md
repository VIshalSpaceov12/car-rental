# bookings module (stub)

Booking lifecycle (`reserved → … → completed`, + `rejected`/`cancelled`),
accept/reject, status transitions, OTP issuance/verification for keyless pickup.
Status enum is owned by `@car-rental/types` (`BookingStatus`). Not implemented yet.
