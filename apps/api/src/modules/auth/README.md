# auth module (stub)

JWT issue/verify, registration, login, social auth, multi-role access control
(customer / service-provider / staff). Not implemented in the skeleton.

Add a `auth.routes.ts` exporting an `authRouter` and mount it in `src/app.ts`.
